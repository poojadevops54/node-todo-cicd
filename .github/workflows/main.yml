name: Test Self Hosted Runner

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Test Runner
        run: echo "Self-hosted runner is working!"
